@model IEnumerable<Hotel_Management_System.Models.Reservation>

@{
    ViewBag.Title = "Indexx";
    Layout = "~/Views/Shared/_Layout.cshtml";
    string ActionName = GlobalVariables.VIEWSHANDLER().ToString();
}

<br />
<div class="container container-fluid">
    <div class="row">
        <p>
            @Html.ActionLink("Add New Reservation", "Create", "Reservations", new { @class = "btn btn-success mr-1" })
        </p>
        <div class="container search-table">
            <div class="search-box">
                <div class="row">
                    <div class="col-md-6">
                        <center>
                            <h5 style="margin-top: 8px;">Search All Fields of Table's Data:</h5>
                        </center>
                    </div>
                    <div class="col-md-6">
                        <input type="text" id="myInput" onkeyup="myFunction()" class="form-control" placeholder="Search all fields of table's data">
                        <script>
                            $(document).ready(function () {
                                $("#myInput").on("keyup", function () {
                                    var value = $(this).val().toLowerCase();
                                    $("#myTable tr").filter(function () {
                                        $(this).toggle($(this).text().toLowerCase().indexOf(value) > -1)
                                    });
                                });
                            });
                        </script>
                    </div>
                </div>
            </div>
            <div class="table-responsive">
                <table id="myTable" class="table table-dark table-striped table-bordered">
                    <thead>
                        <tr>
                            <th>
                                @Html.DisplayNameFor(model => model.Customer.Customer_First_name)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Date_In)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Date_Out)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Days_Interval_of_Stay)
                            </th>
                            <th>
                                @Html.DisplayNameFor(model => model.Transaction.Transaction_Name)
                            </th>
                            <th></th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Customer.Customer_First_name)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Date_In)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Date_Out)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Days_Interval_of_Stay)
                                </td>
                                <td>
                                    @Html.DisplayFor(modelItem => item.Transaction.Transaction_Name)
                                </td><td>
                                    @Html.ActionLink("Edit", "Edit", new { id = item.Reservation_ID }, new { @class = "btn btn-primary mr-1" })
                                    @Html.ActionLink("Details", "Details", new { id = item.Reservation_ID }, new { @class = "btn btn-warning mr-1" })
                                    @Html.ActionLink("Delete", "Delete", new { id = item.Reservation_ID }, new { @class = "btn btn-danger mr-1" })
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
            <p>
                @Html.ActionLink("Back-to-Main-Customer's-List", ActionName, "CUSTOMERS_VIEW", new { area = "" }, new { @class = "btn btn-primary" })
            </p>
        </div>
    </div>
</div>
